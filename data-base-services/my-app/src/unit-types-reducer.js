

const initialState = {
 name: '',
 length: '',
 width: '',
 height: ''
};
export default function unitTypesDetails(state = initialState, action) {
  switch (action.type) {
    case "SET":
      return state
    default:
      return state
  }
}